import 'package:flutter/material.dart';

class OverdueScreen extends StatelessWidget {
  const OverdueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Overdue Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
